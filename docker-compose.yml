version: '3.8'

services:
  tron-server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "9000:9000"
    environment:
      - TRON_PORT=9000

  tron-dashboard:
    build:
      context: .
      dockerfile: dashboard/Dockerfile
    ports:
      - "8501:8501"
    environment:
      - TRON_API_BASE=http://tron-server:9000
    depends_on:
      - tron-server

  tron-worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    depends_on:
      - tron-server
    environment:
      - TRON_SERVER=http://tron-server:9000
    deploy:
      replicas: 2
